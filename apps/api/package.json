{
  "name": "api",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@furtherverse/eslint-config": "workspace:*",
    "@furtherverse/typescript-config": "workspace:*",
    "hono": "catalog:"
  },
  "devDependencies": {
    "wrangler": "catalog:"
  }
}
